import React from 'react';
import { Typography } from '@mui/material';

const MealPlan = () => {
  return (
    <div>
      <Typography variant="h5" gutterBottom>
        This is meal plan render
      </Typography>
      {/* Add further content here */}
    </div>
  );
};

export default MealPlan;
